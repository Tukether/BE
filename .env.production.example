# 프로덕션 환경 설정 (Oracle Cloud 인스턴스용)
# 이 파일을 /home/ubuntu/BE/.env.production 에 저장하세요

# 환경 구분
DJANGO_ENV=production

# Oracle Cloud Vault Secret OCID
# Vault에서 Secret을 생성한 후 각 Secret의 OCID를 복사해서 입력하세요
VAULT_SECRET_KEY_OCID=ocid1.vaultsecret.oc1.ap-chuncheon-1.xxxxxxxxxxxx
VAULT_DB_PASSWORD_OCID=ocid1.vaultsecret.oc1.ap-chuncheon-1.xxxxxxxxxxxx

# 데이터베이스 설정 (민감하지 않은 정보)
DB_ENGINE=django.db.backends.mysql
DB_NAME=tuk_community
DB_USER=dbuser
DB_HOST=localhost
DB_PORT=3306

# 허용된 호스트 (쉼표로 구분)
ALLOWED_HOSTS=your-domain.com,132.226.xxx.xxx

# CORS 허용 도메인 (쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com
